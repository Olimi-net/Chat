<UserControl x:Class="ChatServer.View.ServerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <Style TargetType="ListView">
            <Setter Property="Margin" Value="3px" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListView Grid.Row="1"
                  Grid.RowSpan="2"
                  ItemsSource="{Binding Path=ListUsers}"/>

        <ListView Grid.Row="1" Grid.Column="2" ItemsSource="{Binding ChatLog}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="1" 
                               Content="{Binding Path=User}" 
                               Background="Azure"/>
                        <TextBlock Text="{Binding Path=Message}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Grid Grid.Column="2" Grid.Row="2" Margin="3px">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="NewMessage" Padding="4px">
                <TextBox.InputBindings>
                    <KeyBinding Command="{Binding SendMessage}" 
                                CommandParameter="{Binding ElementName=NewMessage}" 
                                Key="Enter" />
                </TextBox.InputBindings>
            </TextBox>
            <Button Grid.Column="1" 
                    Content="Ок" 
                    Margin="2,0,0,0"
                    Padding="4px"
                    CommandParameter="{Binding ElementName=NewMessage}"
                    Command="{Binding SendMessage}"/>
        </Grid>

    </Grid>
</UserControl>
